# Local dev helpers only — MongoDB runs on Atlas (see MONGO_URL in .env).
# Redis is optional; the API falls back to inline job processing when Redis is down.
services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
